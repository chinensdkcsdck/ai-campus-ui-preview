# AI 校园通 · 客户端与管理端形态及 UI 原型要求

> 文档版本：V1.0  
> 更新日期：2026-07-24  
> 说明：本文只定义产品形态、入口规则与 UI 原型要求，不包含实现代码。

**相关文档：**

- [产品总览](./00-产品总览.md)
- [账号登录与多校体系](./01-账号登录与多校体系.md)
- [管理后台](./07-管理后台.md)
- [页面、指标、MVP 与验收](./09-页面指标MVP与验收.md)
- [UI 原型实施规划](../plans/2026-07-24-ui-prototype.md)

---

## 1. 端形态结论

| 端 | 载体 | 使用者 | 职责 |
| --- | --- | --- | --- |
| 用户端 | **微信小程序** | 学生/普通用户 | AI、广场、失物、活动、交易、消息、我的 |
| 学校管理端 | **独立 Web 后台（PC 优先）** | 校园代理人、审核、知识维护 | 待整理求助、内容审核、知识库、本校配置 |
| 平台控制台 | **独立 Web 控制台** | 平台总部 | 开通学校实例、版本、监控、配额 |
| 轻运营（可选，非 R1 必做） | 小程序内管理待办入口 | 审核员手机处理 | 仅轻量处理；复杂操作回 Web |

### 1.1 为什么管理端以 Web 为主

- 求助整理、知识编辑、批量审核、配置项更适合表格与大屏；
- 扩展到大量学校后，运营密度上升，小程序不适合作为主管理界面；
- 用户小程序保持消费与发布体验，不与重管理功能耦合成单一巨型应用。

### 1.2 第一轮范围

第一轮 UI 设计/原型必须同时覆盖：

1. 用户微信小程序主路径；
2. 学校管理 Web 关键闭环（至少：待整理求助、候选知识审核、内容治理入口）；
3. 平台控制台概念页（学校实例列表、开通实例、运行/版本状态）。

---

## 2. 多学校与小程序入口规则（符合「一校一平台」）

### 2.1 产品原则（不可破）

- 每所学校是独立平台实例：独立品牌、用户、知识库、社区、交易与后台数据；
- **前台不提供「切换学校」**；
- 学校归属由**访问入口**确定，不是用户在首页挑选；
- 底层可复用一套代码与升级机制，但不能做成「全国统一社区」。

### 2.2 「一套小程序代码」不等于「入口都一样」

| 层 | 是否相同 | 说明 |
| --- | --- | --- |
| 代码与版本 | 可相同 | 一套小程序工程，统一发版/灰度 |
| 服务端逻辑 | 可相同 | 强制 `school_id` 隔离 |
| **入口** | **必须可区分** | 每校专属小程序码/公众号菜单/分享卡片/邀请短链等 |
| **进门后的品牌与数据** | **必须不同** | 名称、Logo、主题、内容只属于当前学校 |

推荐表述：

> 同一套程序，不同的进门方式；进门后只看见本校，且不能切换到其他学校。

### 2.3 用户可感知的入口体验

- A 大学用户通过「A 大学专属入口」进入，看到 A 大学品牌与数据；
- B 大学用户通过「B 大学专属入口」进入，看到 B 大学品牌与数据；
- 两边可以是同一小程序 AppID 下的不同启动参数，但产品体验必须是本校独立平台；
- **禁止**默认做成：打开同一首页 → 先选学校再逛全站。

### 2.4 无学校参数进入时的处理

当用户从「最近使用 / 搜索」等路径进入且缺少学校上下文时：

1. 若账号已绑定唯一学校实例：直接进入该校（仍无跨校切换）；
2. 若未绑定：进入「通过邀请码 / 专属码 / 校园投放入口加入」说明页；
3. 该页不是可浏览的学校广场，不展示各校内容流；
4. 服务端最终以会话绑定的学校实例为准，不信任客户端随意改 `school_id`。

### 2.5 规模化到大量学校

- **不要**为每所学校长期维护独立分叉代码；
- **不要**把「一校一 AppID」当作首期必选项（运维成本高）；
- 优先：一套代码 + 实例配置（品牌/开关/配额）+ 入口参数绑定；
- 平台控制台负责批量开通与版本策略；学校后台只管理被授权实例。

---

## 3. UI 原型总要求

### 3.1 目标

UI 设计必须让评审者**在原型上点完主流程**，而不是只看静态线框。要求：

- 全流程可展示、可点击跳转；
- 关键页面内置**演示数据**（学校、用户、帖子、失物、活动、商品、会话、待整理队列等）；
- 演示数据写进设计说明/原型标注，保证开发与评审使用同一套故事线。

### 3.2 演示学校与账号（默认故事）

| 字段 | 演示值 |
| --- | --- |
| 学校 | 东湖大学（`sch_eastlake`） |
| 校区 | 主校区 / 南校区 / 深圳校区 |
| 当前用户 | 林晓，主校区 |
| 可信度文案 | 注册 186 天 · 完成 3 次交易 · 无公开违规 |
| 身份提示 | 非学校官方平台 · 普通用户身份未核验 |
| 专业信息 | 用户自述 · 计算机学院 2023 级 |

可并列展示另外两所学校卡片（南岭学院、湾区理工）仅用于**平台控制台**列表示意，不出现在用户端切换器。

### 3.3 必须可点通的故事线

1. **专属入口登录**：进入东湖大学实例，无学校切换。
2. **AI 有答案**：问「东校区图书馆几点关门」→ 官方资料标签 + 来源 + 反馈。
3. **AI 无答案转广场**：问课程作业体验类问题 → 无法确认 → 若有相关帖则弹出帖子与评论区（可评论/发新帖），否则草稿确认发帖 → 帖子详情评论。
4. **管理员整理结案**：管理端对待整理求助整理验证 → 用户端帖子展示「管理员已整理」（不依赖发帖人点已解决）。
5. **AI 路由闲置**：「想收一个四六级耳机」→ 闲置列表带关键词与校区。
6. **广场顶部失物**：广场顶部失物模块 → 失物列表/发布。
7. **闲置私信**：商品详情 → 平台内私信/消息请求。
8. **活动推广**：活动详情含「推广」标识与报名。
9. **平台开通学校**：控制台创建/查看学校实例与入口形态。

### 3.4 用户小程序信息架构

底部 Tab：

```text
首页 | 广场 | 发布 | 消息 | 我的
```

- 首页：AI 第一入口 + 聚合推荐；
- 广场：顶部失物模块 + 统一动态流（无话题分类）；
- 发布：动态 / 失物 / 闲置 / 活动（按权限）；
- 消息：私信 | 通知；
- 我的：资料、可信度、发布与业务入口；
- 交易、活动从首页/AI/搜索进入，不强制占满底栏。

### 3.5 学校管理 Web 关键页

- 待整理求助队列（主闭环）；
- 候选知识审核；
- 内容与举报处理入口；
- 校区内容共享配置；
- 仅本学校实例数据，无跨校浏览。

### 3.6 平台控制台关键页

- 学校实例列表（状态/版本/用户量/入口类型）；
- 开通学校实例；
- 版本与运行状态（概念级即可）；
- 默认不可查看学校业务正文（帖子/私信/订单等）。

### 3.7 设计表达约束

- 用户端视觉遵循微信小程序常见导航与组件节奏；
- 管理端为中后台工具风（高信息密度、表格/队列优先）；
- 固定露出：非官方声明、未核验身份、AI 来源与时间、活动「推广」、可信度非身份保证；
- 主色建议沉稳学院蓝绿，避免营销风大面积紫渐变。

### 3.8 演示数据最低清单

原型或设计稿中应稳定出现：

- 4 条快捷问题；
- 至少 3 类 AI 回复态：有来源 / 无答案 / 模块路由；
- 至少 3 条失物、4 条广场帖（含 1 条已整理）；
- 至少 3 个活动（含 1 个推广）；
- 至少 3 个闲置商品、2 个数字商品；
- 至少 2 个私信会话（含 1 个消息请求）、3 条通知；
- 管理端至少 3 条待整理/已整理队列；
- 控制台至少 3 个学校实例。

---

## 4. 与实现的边界

- 本文与配套规划文档定义「做什么、怎么演示」；
- **当前阶段仅维护文档，不在本文要求外直接写业务代码**；
- 若后续启动可视化原型，以本文故事线与演示数据为准，目录与技术选型见规划文档。

---

## 5. 验收口径（文档/设计评审）

评审通过需同时满足：

1. 能说清三端分工与多校入口规则；
2. 用户端不出现学校切换器；
3. 广场顶部失物、管理员整理结案、AI 三态在设计中可见；
4. 演示数据齐全且贯穿故事线；
5. 管理端与控制台权限边界在界面文案上可感知。
