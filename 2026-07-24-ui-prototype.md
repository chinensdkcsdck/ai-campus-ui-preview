# AI 校园通 UI 原型 Implementation Plan

> **For agentic workers:** 按任务顺序实现；每完成一屏做一次可点击走查。步骤用 checkbox 跟踪。

**Goal:** 做出可点击原型：微信小程序用户端主路径 + 学校管理 Web 关键闭环 + 平台控制台（多校开通）概念页，支撑后续扩展到大量学校实例。

**Architecture:** 三端分治原型——用户微信小程序、学校管理 Web、平台控制台 Web。多校用“一套代码 + 学校实例配置 + school_id 强隔离”，不为每校复制小程序。管理重能力放 Web；小程序侧仅保留可选轻运营入口。

**Tech Stack:** 静态可点击 HTML 原型（用户端 390 小程序画板 + 管理端 1440 Web）。正式实现时用户端微信小程序，管理端与控制台为 Web（建议同一设计体系，不同应用）。

## Global Constraints

- 一校一实例；页面需有学校品牌位 +「非学校官方平台」提示
- 普通用户无学籍认证标识；学院/专业等为「用户自述」
- 校园广场无话题分类；失物招领挂在广场页顶部模块
- AI 无答案求助由管理员整理结案，不依赖用户点「已解决」
- 个人闲置信息撮合；数字商品才有支付/交付（原型用状态演示）
- 优先用户端 P0；管理端只做求助整理/知识审核必要页
- 文案与信息架构以 `docs/prd/` 为准

---

---

## 0. 已确认产品形态（2026-07-24）

### 用户端
- **微信小程序**（不是先做 H5 站点当主端）
- 面向学生/普通用户：AI、广场、失物、活动、交易、消息、我的

### 管理端（分层，不要做成一个大后台）

| 层级 | 使用者 | 推荐载体 | 原因 |
| --- | --- | --- | --- |
| **L1 学校业务管理** | 校园代理人、审核、知识维护 | **独立管理 Web（PC 优先）** | 表格、批量审核、知识编辑、长表单在小程序里极痛苦 |
| **L2 校园轻运营** | 代理人手机上处理待办 | **同一小程序内「管理入口」或独立管理小程序（可选）** | 推送、抢审、看队列可以；复杂配置仍回 Web |
| **L3 平台控制面** | 平台总部 | **独立系统控制台 Web** | 开通几千学校、版本灰度、监控、配额；绝不能和学校业务后台混权 |

**结论：**
1. **第一轮就要做管理能力**，但主管理界面应是 **Web 管理端**，不是把完整后台塞进用户小程序。
2. 用户小程序里最多放「管理员待办入口 / 简易处理」（可选），完整审核与配置走 Web。
3. 面向几千学校时，**平台控制台** 与 **学校管理后台** 必须分开：前者管实例与系统，后者管本校业务内容。

### 多校扩展到几千所：小程序怎么适配

**不要**为每所学校申请、审核、发布一个独立小程序（运维与版本会爆）。

推荐模型：

```text
一套用户小程序代码（可多环境发布）
  + 学校实例配置（品牌/主题/功能开关/入口参数）
  + 服务端强制 school_id 隔离

一套学校管理 Web（按登录账号绑定可管理的学校实例）
一套平台控制台 Web（仅平台方：开通学校、版本、监控）
```

入口如何区分学校（可并存）：
- 小程序码 / 太阳码带 `school_id` 或短码
- 公众号菜单、分享卡片 scene
- 校园专属搜索名 + 首登绑定（需防串校）
- 后续：半屏插件/第三方平台模式（若走服务商批量代开）

品牌：名称、Logo、主题色、首页配置走实例配置下发，不靠分叉代码。

### 第一轮原型/方案范围调整

R1 同时覆盖：
1. **用户小程序关键页**（主路径）
2. **学校管理 Web 关键页**（待整理求助、候选知识、内容审核）
3. **平台控制台概念页 1–2 屏**（开通学校实例、实例列表/状态）——先讲清模型，交互可浅

原型表达方式：
- 用户端：按**微信小程序尺寸与组件习惯**做可点击 HTML 原型（模拟导航栏/TabBar）
- 管理端：按 **1440 Web 后台** 做可点击 HTML 原型
- 仍不接真后端，但信息架构按三端拆分，避免以后推倒重来

## 1. 为什么这样做原型

这阶段目标不是写完整前端工程，而是：

1. 验证信息架构是否顺手（AI 入口、广场+失物、交易双频道）
2. 把 PRD 抽象流程变成可点路径，方便协作对齐
3. 给后续正式开发当页面清单和交互样板

**推荐形态：移动端 HTML 高保真可点击原型**

| 方案 | 适合 | 不适合 |
| --- | --- | --- |
| Figma 静态 | 视觉精修、对外演示 | 流程多时维护成本高 |
| **HTML 可点击原型（推荐）** | 主路径验证、开发对照 | 极致动效品牌片 |
| 直接上 Vue/React 工程 | 马上要开发联调 | 需求还在改时返工大 |

结论：先 HTML 原型锁交互，确认后再脚手架工程化。

---

## 2. 原型范围（分三轮）

### R1：必须先做（用户主路径，约 12–15 屏）

| 页面 | 要验证什么 |
| --- | --- |
| 登录/注册 | 无学籍认证、可信度提示 |
| 首页 | AI 提问框优先；活动/广场/交易入口 |
| AI 对话页 | 有来源回答 / 无答案 / 模块路由卡片 |
| AI 无答案 → 发帖确认 | 草稿预填、用户确认后发帖 |
| 校园广场 | 顶部失物模块 + 下方统一动态流 |
| 帖子详情 | 评论回答；展示管理员整理状态 |
| 失物列表/发布 | 结构化字段、丢失/拾到 |
| 活动列表/详情 | 报名、推广标识 |
| 交易：闲置列表/详情 | 撮合、私信入口 |
| 交易：数字商品详情 | 预览、价格、购买态（mock） |
| 消息：私信+通知 | 消息请求、未读 |
| 我的 | 发布/收藏/可信度/设置入口 |

### R2：增强

- 闲置/数字商品发布页
- 活动发布与组织认证提示
- 支付成功 / 已购内容（状态页）
- 账号可信度说明页
- 首页活动推广弹窗（可关闭、限频示意）

### R3：管理端精简

- 待整理求助队列
- 候选知识审核
- 内容举报处理
- 校区共享配置（一页说清即可）

**首轮不做：** 复杂数据看板、完整财务结算、跨校控制台、像素级多主题换肤。

---

## 3. 信息架构与导航

### 底部 Tab（用户端）

```text
首页 | 广场 | 发布 | 消息 | 我的
```

- **首页**：AI 第一入口 + 聚合推荐
- **广场**：失物顶部模块 + 动态流
- **发布**：中心加号，展开：动态 / 失物 / 闲置 / 活动（按权限）
- **消息**：私信 | 通知
- **我的**：资料与业务入口

交易、活动不强制占底栏，从首页入口 + 搜索 + AI 路由进入。

### 关键页面关系

```text
首页 AI 框
  ├─ 有答案 → 引用卡片 / 反馈
  ├─ 任务意图 → 跳转闲置/活动/失物（带筛选）
  └─ 无答案 → 帖子草稿 → 广场详情

广场页
  ├─ 顶部失物模块 → 失物列表/发布
  └─ 动态流 → 帖子详情 → 私信（次要）

管理端（独立入口，不进用户 Tab）
  └─ 待整理求助 → 整理验证 → 标记已解决 → 候选知识
```

---

## 4. 设计方向

**校园生活工具感**，不是炫技 AI 营销站。

- 主色：沉稳学院蓝/蓝绿（避免整页紫渐变）
- 字体：系统无衬线；中等信息密度
- 组件：底栏、卡片圆角 8–12px、主按钮、标签（官方资料/校园经验/推广）
- 固定元素：学校品牌、非官方/未核验提示、AI 来源时间、活动「推广」标识
- 原型先做 1 所示例学校

---

## 5. 目录规划

```text
# 规划中的目标结构（尚未作为正式交付）
prototype/  # 或重启时重建

  index.html                 # 三端入口导航
  assets/
    mini.css / admin.css
    mock.js
  mini/                      # 微信小程序用户端原型（390）
    login.html
    home.html
    ai-chat.html
    ...
  school-admin/              # 学校管理 Web（1440）
    dashboard.html
    help-queue.html          # 待整理求助
    knowledge-review.html
    content-moderation.html
    campus-share.html
  platform-console/          # 平台控制台 Web
    schools.html             # 学校实例列表/开通
    school-create.html
    release-status.html      # 版本/运行状态（概念）
  README.md
```

---

## 6. 必须串通的 5 条主故事线

1. **AI 有答案**：问图书馆闭馆时间 → 官方资料 + 来源 + 反馈
2. **AI 无答案 → 广场 → 管理员整理**：无法确认 → 相关帖弹层（评论/发新帖）或草稿发帖 → 评论 → 管理端结案态
3. **AI 路由到交易**：「想收四六级耳机」→ 闲置列表带关键词/校区
4. **广场顶部失物**：顶部摘要 → 发布丢失 → 列表状态
5. **闲置私信**：商品详情 → 私信请求/会话

---

## 7. 任务拆分

### Task 0: 设计基线
- [ ] 390 画板、色板、组件（按钮/卡片/标签/底栏）
- [ ] mock 数据：用户、帖子、失物、活动、商品、会话、待整理求助
- [ ] 页面地图

### Task 1: 壳层 + 登录 + 首页
- [ ] 登录非认证文案
- [ ] 首页 AI 大输入框 + 快捷问题 + 聚合入口
- [ ] 底栏切换

### Task 2: AI 对话与路由
- [ ] 有来源 / 证据不足 / 操作卡片 三态
- [ ] 无答案 → 发帖确认
- [ ] 跳转闲置/活动/失物

### Task 3: 广场 + 失物 + 详情
- [ ] 顶部失物模块 + 统一动态流
- [ ] 详情评论 + 管理员已整理样例
- [ ] 失物结构化发布

### Task 4: 活动 + 交易 + 消息 + 我的
- [ ] 推广标识与报名态
- [ ] 闲置安全提示 + 私信
- [ ] 数字商品购买态 mock
- [ ] 消息请求/通知；我的可信度入口

### Task 5: 管理端最小闭环
- [ ] 待整理求助队列
- [ ] 整理表单与用户端「已整理」态对应

### Task 6: 走查
- [ ] 5 条故事线点通
- [ ] 合规文案位检查
- [ ] 歧义回写 PRD 或原型注释

---

## 8. 协作与时间

| 角色 | 分工 |
| --- | --- |
| 你 | 优先级、文案、校园场景是否成立 |
| Remohdg | 可并行活动/交易/管理端页 |
| 仓库 | 文档在 `docs/`；原型草稿暂存 `prototype/` |

| 阶段 | 预估 |
| --- | --- |
| Task 0–1 | 0.5–1 天 |
| Task 2–3 | 1 天 |
| Task 4 | 1 天 |
| Task 5–6 | 1 天 |
| **R1 可用原型** | **约 3–4 天** |

---

## 9. 明确不做

- 真实登录/支付/AI 接口
- 完整中后台与财务
- 小程序原生（先 H5，后迁）

---

## 10. 已确认选择

1. **端形态**：用户端 = 微信小程序；管理 = 以 Web 为主（学校后台 + 平台控制台）
2. **视觉**：用户端贴近微信小程序规范；管理端为干净的中后台工具风
3. **R1 含管理端**：是——学校 Web 管理闭环必做；平台控制台做开通/列表概念页

---

## 11. 推荐下一步

执行 Task 0：
1. 定小程序导航与 Web 管理信息架构
2. 出三端页面地图（user mini / school admin / platform console）
3. 先做用户小程序壳 + 学校管理 Web 壳，再填 AI/广场与待整理求助主路径

---

## 当前执行约束（2026-07-24 更新）

- **现阶段只修改文档，不写实现代码/不继续堆原型工程代码。**
- 形态、入口、全流程演示与演示数据要求已沉淀至：`docs/prd/10-客户端与管理端形态及UI原型要求.md`。
- 待产品确认后再启动可视化原型制作；启动时必须以该文档的故事线与演示数据为准。

